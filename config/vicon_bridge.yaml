vicon:
  #- roslaunch moticon_insoles show_urdf_everything.launch use_gui:=false
#"roslaunch ximu3_ros ximu_lower.launch do_calibration:=true parent_frame_id:=subject_heading wait_to_start:=true"
  - roslaunch vicon_rviz_markers ik_upper.launch model_file:=$MODEL_FILE name_tag:=$NAME_TAG machine:=$MACHINE wait_to_start:=true filter_output:=$FILTER_OUT
  - roslaunch vicon_bridge vicon.launch #DISABLE_ROS1_EOL_WARNINGS=true roslaunch vio_launch rviz.launch 
  - roslaunch vicon_rviz_markers vis_ik.launch model_file:=$MODEL_FILE filter_output:=$FILTER_OUT
    #machine:=main_pc_docker this is already the default machine for things to run on!
    #  - if [ "${COMBINED_ACQUISITION}" = True ] ;  then rqt --perspective-file /catkin_ws/src/ros_biomech/acquisition_state_machines/rqt_acquisition/Control_Acquisition_small_tabs.perspective    ; fi
  - if [ "${VICON_DUMMY}" = True ] ;  then roslaunch vicon_rviz_markers yaml_markers.launch    ; fi
  - rosrun vicon_rviz_markers viewer_node
